services:
  postgres:
    image: postgres:17-alpine
    container_name: rinklink-postgres
    environment:
      POSTGRES_DB: rinklink
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rinklink"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    image: python:3.11-slim
    container_name: rinklink-backend
    working_dir: /workspace/backend
    depends_on:
      mailpit:
        condition: service_started
      postgres:
        condition: service_healthy
    env_file:
      - ./backend/.env.example
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/rinklink
      MEDIA_ROOT: /workspace/backend/media
      CORS_ORIGINS: http://localhost:5173,http://localhost:5174
      FRONTEND_URL: http://localhost:5173
      APP_ENV: development
      AUTH_ENABLED: "true"
      AUTH_BYPASS_DEV_ONLY: "false"
      AUTH_REQUIRE_MFA_FOR_PRIVILEGED: "false"
      AUTH_JWKS_URL: http://auth-service:3000/.well-known/jwks.json
      AUTH_ISSUER: http://localhost:3000
      AUTH_AUDIENCE: rinklink-api
      EMAIL_FROM_NAME: RinkLink
      EMAIL_FROM_ADDRESS: no-reply@rinklink.local
      SMTP_HOST: mailpit
      SMTP_PORT: 1025
      SMTP_STARTTLS: "false"
      SMTP_USE_SSL: "false"
    command: >
      sh -c "pip install --no-cache-dir -r requirements.txt &&
      alembic upgrade head &&
      uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/workspace/backend

  auth-service:
    image: node:20-alpine
    container_name: rinklink-auth-service
    working_dir: /workspace/auth-service
    depends_on:
      mailpit:
        condition: service_started
      postgres:
        condition: service_healthy
    env_file:
      - ./auth-service/.env.example
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/rinklink?options=-c%20search_path%3Dauth
      BETTER_AUTH_URL: http://localhost:3000
      FRONTEND_URL: http://localhost:5173
      TRUSTED_ORIGINS: http://localhost:5173,http://localhost:5174
      API_AUDIENCE: rinklink-api
      APP_ENV: development
      BETTER_AUTH_SECRET: local-dev-secret-change-this-to-a-long-random-value
      EMAIL_FROM_NAME: RinkLink
      EMAIL_FROM_ADDRESS: no-reply@rinklink.local
      SMTP_HOST: mailpit
      SMTP_PORT: 1025
      SMTP_STARTTLS: "false"
      SMTP_USE_SSL: "false"
    command: >
      sh -c "npm install &&
      npm run dev"
    ports:
      - "3000:3000"
    volumes:
      - ./auth-service:/workspace/auth-service

  mailpit:
    image: axllent/mailpit:v1.27
    container_name: rinklink-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  postgres-data:
